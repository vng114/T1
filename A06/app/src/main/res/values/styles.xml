<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="descriptionItemText">
        <item name="android:textColor">@color/description_item_color</item>
        <item name="android:textSize">@dimen/description_item_text</item>
    </style>
    <style name="titleItemText">
        <item name="android:textColor">@color/title_item__color</item>
        <item name="android:textSize">@dimen/title_item_text</item>
    </style>
</resources>
